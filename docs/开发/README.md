# 开发文档索引

> **总进度与当前唯一下一步：`_A-Plan-总览.md`。** 本索引只说明阅读层级与封存边界。

## 1. 开始当前工作前的阅读顺序

1. 根目录 `AGENTS.md` / `CLAUDE.md`：硬约束与目录边界；
2. `_A-Plan-总览.md`：确认唯一下一步、授权与禁止项；
3. `开发规范.md`：实现、安全、测试、文档和 Git 规则；
4. `治理-DevOps-Copilot-MVP重定位/README.md` 与 `design.md`，以及当前工作包 `HANDOFF.md`（如有）。

## 2. 文档层级

| 层级 | 位置 | 作用 | 不负责 |
|---|---|---|---|
| A | `_A-Plan-总览.md` | 总进度、执行顺序、唯一下一步 | 具体 API/交互设计 |
| A | `开发规范.md` | 完整研发、工作包、文档与安全规则 | 项目进度 |
| A（产品） | `治理-DevOps-Copilot-MVP重定位/` | 当前 DevOps Copilot 定位、P4.0 靶场与闭环设计 | 未确认的实现承诺 |
| B | `_B-V1产品化开发计划.md` | MVP 工作包范围、边界与验收 | 独立维护下一步 |
| B（实施） | `P4-DevOps-Copilot-MVP/` | 已完成 Work 1 的设计、实现、审查与关闭交接 | 覆盖 A-Plan |
| 历史 | `M*/`、`P0*/`、`P1*/`、`P2*/`、`P3*/`、`初始开发/` | 技术事实、实验和演进记录 | 当前产品需求 |

## 3. 当前 P4 的正确读取方式

- `治理-DevOps-Copilot-MVP重定位/`：当前产品定位与 P4.0 入口；
- `P4-DevOps-Copilot-MVP/`：Work 1 已在 2026-07-30 通过真实 PostgreSQL 靶场 smoke 并完成收口；
- Work 1 的唯一受控目标是 `127.0.0.1:5433/opermind_demo` 的 `opermind_demo` schema；不得操作 `gongkar`；
- P4.1 尚未获得 Design/Review/用户授权，不能提前改 Agent、API、前端或产品执行器。

## 4. 封存边界

| 路径 | 状态 |
|---|---|
| `治理-个人AI运维助手产品重定位/`、`治理-产品定位复核/` | 已封存的产品探索和研究快照；可引用，不可执行。 |
| `P3-主前端工作台/`、P3.5/P3.6 文档 | 已封存的个人会话与发送恢复路线；代码/测试事实可选择性复用。 |
| `M0-M7/`、`report/`、`experiments/` | 研发、Trace、评测和毕业设计材料；保留，不定义产品主线。 |

## 5. 维护规则

- 只允许 A-Plan 使用“当前唯一下一步”。
- 新活文档归属：产品重定位放 `治理-*`；P4 及后续实现放 `P*-*`；规则放 `开发规范.md`；进度放 A-Plan。
- 一个工作包可以含 1–3 个紧密相关切片，在完成时集中 Test → Review → Commit；安全、架构、公开契约、迁移、真实数据源和破坏性改动仍须独立设计与审查。
- 完成/封存的 `HANDOFF.md` 必须标明状态和新的恢复入口；不可保留过期“待提交”“待用户审阅”或“当前下一步”。