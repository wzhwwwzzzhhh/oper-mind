# P4.3 HANDOFF — 已注册服务中心、有限监控与调查入口

> 更新：2026-07-31　|　状态：**实现、回归与实现审查完成；真实 target smoke 待当前进程靶场凭据后补验收**

## 当前产品基线

- P4.0、P4.1、P4.2 已完成。P4.3 已将既有“会话调查 → 固定 Proposal → `local_operator` 审批 → 白名单执行 → Verify”接入主产品的服务入口。
- 新增唯一静态服务 `order-service`（订单服务靶场）和有限服务详情页；它不是通用监控平台，也不支持用户接入服务、连接串或凭据。
- 服务详情的 target 快照只读取 P4.1 已批准的 health、metrics、固定 DB 索引/计划事实；不读取日志/probe，不执行故障注入、DDL/DML 或 action executor。
- 服务入口创建 `service_id=order-service` Session 并预填慢查询问题，用户仍需点击 Workbench 的“开始调查”。未点击前没有 Message、Run、Result、Proposal 或外部 target 读取。
- 绝不探测、读取、写入或清理 `gongkar`；唯一可操作目标仍是授权 `opermind_demo` 靶场。

## 已完成的实现

- 静态领域/Connector/服务应用层：`backend/src/domain/services.py`、`backend/src/application/service_center.py`、`backend/src/infrastructure/diagnosis/demo_orders/service_connector.py`。
- 会话关联和安全活动查询：迁移 `20260731_03_p4_service_center`，`sessions.service_id` 仅允许 `NULL/order-service`，服务活动只读取绑定会话的最小摘要。
- API：`GET /api/v1/services`、`GET /api/v1/services/{service_id}`、`GET /api/v1/services/{service_id}/activities`、`POST /api/v1/services/{service_id}/sessions`。
- 产品 UI：服务列表、详情、有限快照、活动列表和跳转到带服务标签/预填问题的 Workbench。
- 回归：后端 `158 passed`；前端 `44 passed`、typecheck/build 通过；OpenAPI TypeScript 已重生成。

## 尚未完成的唯一验收

`backend/scripts/smoke_p4_service_center.py` 已实现固定路径：

```text
start → inject → GET 服务快照 → 创建服务会话 → 用户发起 P4.1
→ Proposal / approve / execute / Verify → GET 快照与活动 → finally clean
```

本次运行因**当前进程未提供靶场凭据**而在配置校验阶段 fail-closed：未连接数据库、未启动靶场、未创建 schema，且未访问 `gongkar`。恢复时只需在授权凭据已存在于当前进程环境的前提下运行该脚本；不得把凭据写入文件、文档、日志或 Git，也不得改为 mock 冒充 target。

## 下一步与禁止事项

1. 先补 P4.3 target smoke；通过后更新本文件、`p4.3-implementation.md`、`p4.3-implementation-review.md` 和 `../_A-Plan-总览.md` 的验收状态。
2. target smoke 通过前，不扩展第二故障、第二服务、用户服务配置、MCP、知识库、RAG、文件上传或用户模型 Key。
3. target smoke 通过后，下一工作包是 P5.0 的独立 Design → Review → 用户授权：受控 Markdown 知识目录和程序全文检索。模型不能调用任意 grep/Shell；不实现向量库、上传或用户知识库。
