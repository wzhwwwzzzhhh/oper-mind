# P4.1 HANDOFF — 会话触发的订单慢查询调查

> 更新：2026-07-30　|　状态：**历史 HANDOFF，P4.1 已完成并关闭；P4.2 已在后续工作包完成**

## 已完成

- 按 P4.1 Design/Review 实现 DB、Log、Server 三个确定性只读调查角色，以及结构化证据、根因、不确定性和角色摘要；
- P4.1 `mock` 模式已通过现有 `/api/v1`、Run 事件/SSE 兼容、Result 持久化与 Workbench 展示验证；
- `target` 只允许专用 `opermind_demo` 靶场，应用元数据库隔离，配置错误或全部证据源不可用时安全失败；
- 前端展示最小过程和可展开的证据、角色摘要、根因、风险；没有 CoT、原始 SQL、日志、HTTP body、绝对路径、异常详情或凭据；
- 非支持问题明确说明“当前 MVP 只支持订单慢查询的只读调查”；不伪造调查，也不回退为旧 Agent 实验回答；
- 不包含审批、修复、DDL/DML、Verify 产品动作、知识库、Provider、服务页或新的公开 API/迁移；
- 已新增 `backend/scripts/smoke_p4_orders_evidence.py`，真实验收固定为 Work 1 `start → inject`、P4.1 API/SSE 调查、Work 1 `repair → clean`。

## 已验证

```powershell
$env:PYTHONPATH = "$PWD\backend;$PWD"
.\.venv\Scripts\python.exe -m pytest backend/tests -q
# 145 passed

Set-Location frontend
npm test
# 40 passed
npm run build
# passed（仅有既有 bundle 大小提示）
```

## 完成验收

2026-07-30 已在专用靶场执行：

```text
start → inject → API/SSE 调查 → repair → clean
```

脚本内部确认 DB / Log / Server 三类证据、`high / 0.95` 缺失索引根因、RunEvent/SSE 终态回放，并在结束后回收专用 schema 和靶场运行时文件。全程未访问 `gongkar`，凭据未写入仓库、文档、事件或结果。

本 HANDOFF 的“P4.2 实现前”描述是当时的历史停止点。P4.2 已于后续工作包完成实现、验证与提交；当前唯一下一步以 `../_A-Plan-总览.md` 为准：P4.3 Design/Review 已完成，等待用户明确授权实施。
