# M8 Step1 — 端到端联调与 demo 录屏

> 状态：⚪ 计划（待开工时填写）

## 计划改动

- 前后端联调（`src/app.py` + `src/frontend/`），修联调暴露的 bug。
- 准备 5 个稳定 mock 演示场景（direct/chain/parallel + debate + reflection 各一）。
- demo 录屏 + 降级兜底（断网/SSE 断线预案）。

## 待填

- [ ] 演示脚本
- [ ] 录屏链接
