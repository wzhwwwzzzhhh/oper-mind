# 文档层级收口 — 执行快照

> 日期：2026-07-29　|　状态：✅ 文档整理完成，待独立审查与逐项提交
>
> 范围：仅整理文档入口、权威层级、过期交接与已失效路径；不修改产品实现、API 契约事实或历史设计结论。

## Design

- 问题：计划在迭代中已多次调整，A-Plan、B-Plan、R1、P0–P3 HANDOFF 与历史日志同时出现“当前/下一步”叙述，恢复任务可能误把历史工作台和已完成交接当作现行指令。
- 目标：建立一个可复现的阅读入口，使“进度”“产品体验”“规则”“技术事实”“历史证据”各自只有明确职责；保留历史，不以删除掩盖演进。
- 非目标：不改 `docs/00-项目方案说明书.md`、R1 `design.md` 的隔离未提交内容；不移动 M/P 日志目录；不改 `/api/v1` 事实、后端、前端或真实资源接入边界。

## Step

1. 以 `git status --short`、最新提交 `6b0290b`、A-Plan、R1 HANDOFF 和 P3 HANDOFF 恢复现场；
2. 盘点 `docs/`，区分活文档、契约/技术基线、开发快照和历史材料；
3. 新增顶层与开发目录索引，明确恢复顺序和权威层级；
4. 收敛 A-Plan/B-Plan 的进度职责，关闭已完成 P0/P1/P2 与旧 P3/R1 研究 HANDOFF；
5. 修复两个 M4 历史日志中已经改名的目录引用，执行链接、空白和状态校验。

## Code

- `docs/README.md`：新增顶层文档导航与五层文档职责；
- `docs/开发/README.md`：新增开发恢复顺序、权威表、P3/R1 读取方式和历史归档规则；
- `docs/开发/_A-Plan-总览.md`：以 `6b0290b` 更新 R1/P3.5 提交状态；唯一下一步收敛为“P3.6a 需用户单独授权”；
- `docs/开发/_B-V1产品化开发计划.md`：删除重复的项目级下一步，改为显式指向 A-Plan；
- `docs/开发/P0-*`、`P1-*`、`P2-*`、`P3-*` 与 `治理-产品定位复核/` 的 `HANDOFF.md`：标记为已关闭或已移交，保留历史证据入口；
- `AGENTS.md`、`CLAUDE.md`、`docs/开发规范.md`：同步索引、单一下一步和 HANDOFF 收口纪律；
- `docs/开发/M4-真实评测基础设施/step2-*`、`step3-*`：将不存在的 `M4-真实模型实验/review.md` 修为现存的本目录 `review.md`。

## Test

- 核对工作区与最近提交：R1/P3.5 已于 2026-07-29 提交为 `6b0290b`；
- `AGENTS.md` 与 `CLAUDE.md` 字节级一致；
- 对本次文档变更执行 `git diff --check`，无新增行尾空白；
- 用脚本扫描 `docs/` 内 Markdown 链接、变更文件尾随换行和权威状态断言；
- A-Plan 恰有一个项目级“当前唯一下一步”章节；B-Plan 不再声明独立的项目级下一步。

## Review

- 结论：通过自检，待按项目工作流完成独立审查后逐项暂存/提交。
- 已知限制：历史快照仍可能包含当时的“当前下一步”、旧行号或旧状态；这属于日期化证据，不在本次批量改写范围。新索引和已关闭 HANDOFF 已明确禁止把它们当现行指令。
- 隔离：未触碰当前未提交的 `docs/00-项目方案说明书.md`、`docs/开发/治理-个人AI运维助手产品重定位/design.md`、`backend/src/domain/__init__.py`、`backend/src/infrastructure/persistence/__init__.py`。
- 提交边界：仅暂存本次列出的文档、`AGENTS.md` 和 `CLAUDE.md`，禁止 `git add .`。
