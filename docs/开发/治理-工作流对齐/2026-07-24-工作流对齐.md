# 工作流对齐 — AGENTS / CLAUDE / 开发规范

> 日期：2026-07-24　|　状态：✅ 通过　|　关联 commit：本提交（见 Git 历史）

## Design

项目同时存在 `AGENTS.md`、`CLAUDE.md` 与 `docs/开发规范.md` 三份开发约定。M6 完成后检查发现：两份入口规则在审查纪律、长期记忆隔离、目录结构和开发日志位置上存在漂移，容易导致不同协作入口执行出不同流程。

目标是建立明确层级：完整规则以 `docs/开发规范.md` 为唯一真相源；`AGENTS.md` 与 `CLAUDE.md` 作为内容完全相同的精简镜像；M5–M8 进度以 `docs/开发/_A-Plan-总览.md` 为唯一真相源。

## Step

1. 对比两份入口规则和 A-Plan 实际目录约定。
2. 合并双方有效约束：step 级 Review、独立 code review、评测长期记忆隔离、API/SSE 契约同步。
3. 将 DDD 日志规则从旧的 `docs/初始开发/` 迁移到 A-Plan 目录；为不归属某个里程碑的规则治理增加独立目录例外。
4. 精确比对 `AGENTS.md` 与 `CLAUDE.md`，并检查 A-Plan 的 M6/M7 状态是否与已提交代码一致。

## Code

- `AGENTS.md:1-71` 与 `CLAUDE.md:1-71`：改为完全一致的镜像内容，明确工作流 `Design → Step → Code → Test → Review → Commit`。
- `docs/开发规范.md:109-194`：定义规则真相源、同步范围、独立审查纪律、A-Plan 日志目录与跨里程碑治理例外。
- `docs/开发/_A-Plan-总览.md:32-44`：M6 标记为完成（提交 `f6eb087`），M7 标记为下一个。

## Test

- 精确字符串比较：`AGENTS.md` 与 `CLAUDE.md` 全文一致。
- `git diff --check`：通过。
- 人工核对：M6 已有 API/SSE 实现与提交 `f6eb087`；M7 仍未创建前端分支，因此状态使用「下一个」而非「进行中」。

## Review

- 审查人：Plato（独立 code-review agent），审查日期：2026-07-24。
- 审查发现与处置：
  1. 原日志在独立审查完成前预先写为「通过」；现已回填本次真实审查结论与处置，保证审查发生在提交前。
  2. `docs/开发规范.md` 开头只提及 `CLAUDE.md`，与双镜像约定冲突；已改为同时明确 `AGENTS.md` 和 `CLAUDE.md`。
  3. M7 step1 曾把前端客户端目录写成根 `src/api/`，可能误导修改 M6 后端契约；已改为 `src/frontend/src/api/`。
- 复核：`AGENTS.md` 与 `CLAUDE.md` 全文精确一致，`git diff --check` 通过；M6 已完成、M7 为下一个的 A-Plan 状态与代码/分支现状一致。
- 结论：**通过**。
