# M7 Step2 — 诊断链路实时视图

> 状态：⚪ 计划（待开工时填写）

## 计划改动文件

- `src/frontend/src/components/trace/*` —— 消费 SSE 事件，实时渲染路由决策 → Agent 起止 → 辩论 → 反思 → 报告。direct/chain/parallel 三种策略视觉差异化（核心看点）。
- 相关 `pages/` 与状态管理。

## 待填

- [ ] 链路视图组件设计（DAG / 时间线形态选型）
- [ ] 事件到 UI 状态的映射
