# M7 Step1 — 前端脚手架与 SSE 客户端

> 状态：⚪ 计划（待开工时填写）

## 计划改动文件

- `src/frontend/` —— Vite+React+TS 初始化：`package.json`、`vite.config.ts`（proxy 到 uvicorn）、`tsconfig.json`、`index.html`、`src/main.tsx`、`src/App.tsx`。
- `src/frontend/src/api/` —— SSE 客户端（EventSource 封装）+ 请求封装。
- `src/frontend/src/types/` —— 与 M6 契约对齐的 TS 类型。

## 待填

- [ ] 脚手架命令与依赖版本
- [ ] `/health` 调通证据
