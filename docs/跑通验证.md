# 跑通验证（WP0）— mock 与真实模式全链路

> 验证日期：2026-08-08（卡点状态更新：2026-08-23，Issue #98 第三轮独立终审 PASS）
> 目的：确认"启动服务 → 建会话 → 发问题 → 多 Agent 调查 → Trace 流出 → 结论落地"端到端真实可跑，mock 与真实两条路径都过，卡点如实记录。
> 前置：依赖安装（`.venv` + `npm install`）与数据库迁移（alembic upgrade head）已完成。
> 定位：本文是**验证记录**（一次验证的事实快照）；待修问题的排期与状态见 `docs/完善清单.md`。

## 环境拓扑

```
┌─ 本机（Windows）─────────────────────────────────────────────┐
│  OperMind 后端（真实模式）: http://127.0.0.1:8001             │
│  OperMind 前端:            http://localhost:5174（Vite 代理 /api → 8001）│
│  finalshell 隧道:          127.0.0.1:5432 ← 服务器 5432       │
├─ 服务器（4G 云主机）─────────────────────────────────────────┤
│  PostgreSQL：opermind_demo 靶场库（public.orders 20 万行）    │
└──────────────────────────────────────────────────────────────┘
```

- 8000 端口是用户自己维护的常驻后端（mock 模式），验证实例独立使用 8001，互不干扰。
- 凭据全部走环境变量注入（`OPERMIND_API_KEY`、`OPERMIND_SERVICE_*_DSN`、`OPERMIND_SECRET_KEY`），不落任何文件。

## 路径一：mock 模式全链路（零外部依赖）

| 步骤 | 命令 / 操作 | 结果 |
|---|---|---|
| 启动后端 | `OPERMIND_API_KEY=mock` + `uvicorn src.app:app --port 8001` | ✅ `/health` 返回 `mode: mock` |
| 启动前端 | `VITE_API_PROXY_TARGET=http://127.0.0.1:8001 npm run dev` | ✅ 5174 可访问 |
| 建会话 | UI/API `POST /api/v1/sessions`（绑定服务） | ✅ 201 |
| 发问题 | S1 慢查询场景（含 SQL 关键词触发 mock 工具调用） | ✅ 202 后台执行 |
| Trace | SSE/轮询 events | ✅ 13 个 trace 事件（route_decided → agent_start → 工具调用 → agent_done） |
| 结论 | 固定 S1 答案落地 | ✅ "status 字段没有索引导致全表扫描" |

**验证结论**：mock 路径完整走通，确定性场景可复现（M3 复现性基础设施的预期行为）。

## 路径二：真实模式全链路（服务器 PG + 真实 LLM）

| 步骤 | 命令 / 操作 | 结果 |
|---|---|---|
| 隧道 | finalshell 建 5432 隧道 | ✅ `pg_isready` / 连接正常 |
| 灌数据 | `public.orders` 20 万行（customer_id、created_at、status 等，确定性生成） | ✅ Seq Scan 已由 EXPLAIN 确认 |
| 模型配置 | 前端 /models 录入 provider → verify → activate | ✅ 201/200，key 经 AES-256-GCM 加密落库（P6），接口只返回 `has_api_key` + `masked_tail` |
| 服务配置 | `OPERMIND_SERVICE_POSTGRES_PRODUCTION_DSN` / `_TARGET_DSN` 指向隧道端口 | ✅ `GET /services` 显示 4 实例（production/staging/target/redis） |
| 调查 1（生产库） | session 绑定 postgres-production，问 orders 慢查询 | ✅ run `e8fbf8f7…`：真实 LLM 路由 → db agent → explain_sql(546ms)/show_create_table(656ms)/show_index(641ms) → Trace 显示 **Seq Scan · Plan Rows 39,793 · Total Cost 4562** → 结论"status 缺索引，建议 CREATE INDEX idx_orders_status" |
| 调查 2（靶场库） | session 绑定 postgres-target，问 customer_id/created_at 慢查询 | ✅ run `976ef291…`：真实 LLM 分析出 **Gather Merge → Sort → Seq Scan（递归确认）**，Total Cost 4532.73，结论"customer_id/created_at 缺复合索引" |
| 受控动作闭环 | 提案 → 审批 → 二次确认执行 → 独立 Verify | ✅ 2026-08-27 受控靶场真实复核：proposal `verified`；固定索引存在且有效；Seq Scan → 目标 Index Scan；8 类审计事件完整 |

**验证结论**：既有真实模式“真实 LLM + 真实数据库 + Trace 证据 + 结论”主链完整；2026-08-27 又以确定性 mock LLM + 真实 PostgreSQL collector/固定 DDL executor/独立 Verify/API/审批/审计路径完成受控动作闭环。成功索引按边界保留，未自动删除。

## 配置最终形态（不含凭据值）

| 配置项 | 位置 | 形态 |
|---|---|---|
| LLM 模型与 Key | 前端 /models 页 | P6 加密存储（AES-256-GCM，主密钥 `OPERMIND_SECRET_KEY` 环境变量） |
| 服务 DSN | 环境变量 | `OPERMIND_SERVICE_<INSTANCE_ID 大写、-→_>_DSN`（P4.4 约定，`config.py:load_service_dsn`） |
| 动作模式 | 环境变量 | 存在 `OPERMIND_SERVICE_POSTGRES_TARGET_DSN` → `load_action_mode()` 返回 `target` |
| 应用数据 | `data/opermind.sqlite3` | 与诊断目标服务完全隔离 |

## 卡点清单

### 未解决

**C2 · demo 与 P5 固定对象不匹配**
- `demo/orders-slow-query/postgres/schema.sql` 建 `opermind_demo.orders(user_id)`，而 P5 执行器硬编码 `public.orders(customer_id, created_at)`。已通过在库层直接建 `public.orders` 绕开；schema.sql 文件修正标记为 worktree follow-up。

**C4 · finalshell 隧道不稳定**
- 验证期间反复断开（至少 3 次），需手动重连；建议开启 keepalive。属环境问题，不影响产品结论。

**C5 · UI 体验问题（已归并进 `docs/完善清单.md`）**
- B1 运行中无"正在思考"提示 → 完善清单 P1-4（仍未处理）；
- B2 回答无 Markdown 渲染 → issue #101 已完成安全 Markdown 自动化、浏览器复验与真实链总验收；
- B3 结构化结果字段为空 → issue #101 已完成白名单模板事实派生与诚实空态；真实缺失索引公开投影
  确认为 1 条建议、3 条数据库证据与非空 impact；
- 归并索引保留在 `docs/backlog.md`，状态以完善清单为准。

### 已解决（排障记录，有面试价值）

| 卡点 | 解决方式 |
|---|---|
| curl 中文 body 解析失败 | git-bash 编码问题 → 用 `-d @file` |
| Idempotency-Key 报 400 | 必须 UUID 格式（`parse_idempotency_key`） |
| SERVICE_CONTEXT_REQUIRED | 服务绑定必须在**建会话**时传 `service_ids`，run 级 `service_id` 不够 |
| Vite HTTP 000 | 监听 localhost（IPv6），用 `localhost` 而非 `127.0.0.1` 访问 |
| 灌数据报错（NotNullViolation / 溢出） | 改为确定性生成 `(ARRAY[...])[1 + id % 5]` + `id::bigint` 乘法 |
| C1 P5 受控动作提案/执行真实闭环 | 两处 `to_regclass` 改存在性判断并补真实链复核；2026-08-27 经人工批准与二次确认后固定 target 执行成功，独立 Verify 确认索引有效且计划由 Seq Scan 转目标 Index Scan；issue #101 再次实测并同时确认结构化建议、证据与影响面真实投影；API 提案终态 `verified`，审计链完整 |
| C3 mock LLM 跨角色假调用 | Issue #98 已改为互斥角色白名单、显式场景工具事实、证据类别错配门禁与失败关闭；29 项行为评测、605 项后端全量、隔离端口 UI 复验及第三轮独立终审全部通过 |
| C6 裁判配置面收口（judge 误导展示） | Issue #104 路径 B 收口（2026-08-27）：`/model/config` `judge_model` 恒为未启用（字段结构保留）、`activate` judge 返回 400 `JUDGE_ENDPOINT_NOT_ENABLED`、存量 judge 行投影"未启用"、前端删除"裁判模型"卡片与"设为裁判"按钮并如实标注质量节点由主诊断模型承担；死代码（fallback.py / `_mock_response`）清理。验证：后端全量 pytest、前端 `typecheck` / `test` / `build`、`git diff --check` 全部通过，UI 复验无裁判误导文案 |

## 待办

- [x] **补验受控动作闭环**（C1）：提案 → 审批 → 二次确认执行 → 独立 Verify 已于 2026-08-27 通过；脱敏证据见 `docs/workpack/P8-controlled-action-closeout/evidence.md`
- [ ] C2 `demo/orders-slow-query/postgres/schema.sql` 修正为 `public.orders(customer_id, created_at)`，与 P5 执行器固定对象一致
- [x] C3 mock LLM 跨 Agent 假调用未注册工具（完善清单 P0-3，2026-08-23 issue #98 第三轮独立终审 PASS）
- [ ] 截图资产整理到 `img/screenshots/`（当前 `img/对话框太短.png` 散落在根）
- [ ] P7 三项功能真实体验验证（完善清单 P7 段）
