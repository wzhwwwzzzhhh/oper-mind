# 完善清单（体验驱动开发工作地图）

> 创建：2026-08-08（全面盘点：后端逐能力 + 前端逐页面 + WP0 跑通验证发现）
> 更新：2026-08-09（按当前工作区实际代码状态核对每条状态；P7 已在 main，不再是"待 merge"）
> 用法：每天起服务体验 → 发现问题就往这里挂（分级）→ 修完更新状态。这份清单是主线，`backlog.md` 是收集入口。
> 标注规则：✅ 已修复（含日期与验证方式）　⏳ 在修或部分修　— 未修。状态纪律见 `开发规范.md` §7.3。

## 分级说明

| 级别 | 含义 | 总数 | 未修 | 在修 | 已修 |
|---|---|---|---|---|---|
| **P0 必修** | 核心能力坏掉 / 数据到了但被丢弃 / 明示功能不存在 | 6 | 3 | 3 | 0 |
| **P1 该修** | 误导用户（假数据/假入口/假配置）/ 体验核心链路缺反馈 | 12 | 10 | 1 | 1 |
| **P2 可缓** | 展示打磨 / 次要体验 | 8 | 8 | 0 | 0 |
| **P7 体验验证** | main 已合入的 P7 功能，需真实体验确认 | 3 | 3 | 0 | 0 |

> 当前工作区有一批未提交改动（P0-1/P0-2/P1-5 相关），已在下方逐条标注为 `⏳`——
> 代码已改且单测通过，但**端到端闭环尚未复验**，按 §7.3 不能标 ✅。

---

## P0 必修（核心能力）

### P0-1 受控动作闭环三重断点 —— 端到端永远跑不通
- **现象**：提案卡永不出现；即使绕过第一处，执行也会 BLOCKED。
- **根因**（三处叠加，修一处不够）：
  1. 收集器 `backend/src/infrastructure/diagnosis/postgres_missing_index.py:33`：`to_regclass('public.orders') != "public.orders"`——真实 PG 默认 search_path 下返回 `"orders"`，恒不等 → 信号永不产出。
  2. 执行器 `backend/src/infrastructure/actions/postgres_target_executor.py:155`：`_table_exists` 同款 to_regclass 比较 bug → 前置条件恒失败。
  3. 组装器 `backend/src/infrastructure/diagnosis/result_assembler.py:62-73`：investigation 为 None 时结构化字段全空（confidence 0.0 / severity INFO / 根因证据空）。
- **修复思路**：① 改 `is None` 存在性判断（两处）；② 测试 mock 值 `"public.orders"` → `"orders"`（`tests/test_p5_controlled_action.py:156`）防再犯；③ 修完后补验完整闭环：提案 → 审批 → 执行 → Verify（EXPLAIN 转 Index Scan）。
- 状态：**⏳ ①② 已完成，③ 未做**（2026-08-09 核对工作区）
  - ① 两处 `to_regclass` 已改为 `is None` / `is not None` 判断，并加注释说明 regclass 按 search_path 简化；
  - ② 测试 mock 已改为 `"orders"`，并新增 4 个收集器用例（简化名产出信号 / 表不存在 / 索引已存在 / 无顺序扫描）；
    `pytest tests/test_p5_controlled_action.py -q` → 11 passed；
  - ③ **真实环境端到端闭环仍未复验**（需隧道 + 靶场库），这是本条能否标 ✅ 的唯一剩余条件；
  - 关联卡点：`docs/跑通验证.md` C1。

### P0-2 结构化诊断结果零产出 —— "结构化"产品承诺未兑现
- **现象**：前端"结构化诊断结果"面板永远显示"服务未返回结构化根因/证据/角色摘要/风险说明"（`frontend/src/features/workbench/DiagnosisResultPanel.tsx:120-141`）；置信度恒 0%、严重度恒 info。
- **根因**：生产默认 `KernelReportResultAssembler` 在 investigation 为 None 时保守留空（`result_assembler.py:39-74`）；investigation 本身被 P0-1 第 1 处 bug 掐死。P1 注释"尚未接入受控工具"已过时。
- **修复思路**：P0-1 修完本项大半自动解决；再评估 assembler 是否需要把 report 正文的结论提炼为结构化字段（设计评审后做，避免"从散文反推事实"违背设计原则——保守留空是安全设计，前端渲染方式也可改进，见 P1-3）。
- 状态：**⏳ 部分修复**（2026-08-09 核对工作区）
  - `risks` 已接线：收集器产出两条确定性范围说明（只读覆盖面 / EXPLAIN 单次采样），assembler 从
    `investigation.risks` 透传，无调查时留空；已补 2 个用例（透传 / 留空）；
  - assembler docstring 已从过时的"尚未接入受控工具"改为"结构化字段只来自确定性只读事实"；
  - **仍空**：`recommendations=[]` 在 `result_assembler.py:69` 仍硬编码空列表——建议来源需设计确认
    （从白名单动作模板派生，而非从报告正文反推）；
  - **仍缺**：真实环境验证结构化字段有值（同 P0-1 ③）。

### P0-3 mock 模式多 Agent 体系整体失真
- **现象**：mock 下 4 个 Agent（db/server/log/knowledge）输出**完全相同**的 canned 结论；非 DB Agent 被网关拒绝 `explain_sql`（`调用 explain_sql 未注册，已拒绝`）自身却无感知；conflict_check 启发式、debate、reflection 在 mock 下全部空转或绕过——"多 Agent 协作"在 mock 下是假的。
- **根因**：`backend/src/core/llm.py:96-114` `_mock_chat` 对任何含 SQL 关键词的消息伪造 `explain_sql` 调用；:76-86 工具结果后返回固定 S1 文案。
- **修复思路**：mock 按 Agent 角色区分（注册了哪些工具就调哪些）；或改为按 `tools` 参数选择要调用的工具名；输出文案带角色差异。
- 状态：—（`docs/跑通验证.md` C3）

### P0-4 前端丢弃已返回的数据：recommendations / impact / report_markdown 零渲染
- **现象**：`frontend/src/api/features/workbench/result-readers.ts:70-85` 完整投影出 `impact`、`recommendations`、`report_markdown`、`requires_approval`，测试 mock 也返回非空值（`frontend/src/test/handlers.ts:218-231、245`），但 `DiagnosisResultPanel` 只渲染 summary/root_causes/evidence/agent_summary/risks/ID——**修复建议、影响面、报告全文三个板块彻底丢弃**。
- **根因**：渲染组件遗漏，非后端问题。
- **修复思路**：DiagnosisResultPanel 补三个板块渲染（recommendations 列表、impact 说明、report_markdown 全文展示）。
- 状态：—

### P0-5 高危 SQL 审批机制未接线（文档承诺 vs 实际行为脱节）
- **现象**：`backend/src/core/approval.py` 有 CLI 交互审批（`input()`），`ToolRegistry.execute_tool` 带审批——但 BaseAgent 全部走 ToolGateway，**此路径永不执行**。"高危 SQL 审批"实际不存在。
- **处置**：**✅ 已删除**（2026-08-10）。`src/core/approval.py` 整文件与 `ToolRegistry.execute_tool` 方法均为全仓库零调用点的死代码，按用户决策删除；审批能力边界留待后续 Design 时重新设计为「提案→审批→白名单执行」接入 ToolGateway 执行链。
- 状态：**✅ 完成**

### P0-6 死代码与未接线能力成片（含配置面/行为面脱节）
- **现象**：
  - `judge_llm` 配置齐全（config + API + 前端展示），**全系统无任何节点使用**——debate/reflection 实际用主 llm（`core/debate.py:47,53`、`core/reflection.py:77` 自注"简化实现"）。
  - `core/fallback.py`（RuleEngine）整文件无引用；`llm.py:122` `_mock_response` 无引用。~~`ToolRegistry.execute_tool` 无引用（见 P0-5）~~ → P0-5 已删除（2026-08-10）。
  - `backend/src/api/v1/dependencies.py:96` 注释"审批执行器仍为空骨架"已过时（执行器已实现）；`result_assembler.py:34` "尚未接入受控工具"已过时。
- **修复思路**：judge_llm 接线到 debate/reflection（或如实删除配置面）；死代码删除或标注；过时注释更新。
- 状态：**⏳ 仅注释部分修复**（2026-08-09 核对工作区）
  - ✅ `result_assembler.py` 过时 docstring 已重写（随 P0-2）；
  - — `dependencies.py:97-98` "审批执行器仍为空骨架，结构化字段保守留空"**仍是过时描述**，
    执行器已实现、风险字段已接线，需同步改；
  - — `judge_llm` 仍只在 config/API/前端展示层出现（`config.py:18-20`、`routes.py:270`、
    `model_providers.py:307`），debate/reflection 依旧用主 llm，**编排层零引用**；
  - — `core/fallback.py`（RuleEngine）仍全项目无引用（`app.py:202` 的 `_env_config_fallback` 是同名不同物）；
    `llm.py` `_mock_response` 同样无引用。

---

## P1 该修（误导 / 核心体验）

### P1-1 全局 3 个死入口/死按钮，点击无任何反应
> 2026-08-09 复核：原记录的 5 个已减为 3 个。GlobalNav 两项在 P7 合入后已接线，见下。

- ✅ `GlobalNav.tsx:16-17,24-25`："服务监控"→`/monitor`、"文档知识库"→`/knowledge` 已有
  `active` 判定与 `go()` 分支，路由真实可达（P7 #48/#50 合入时一并接线）。
- — `ServiceContextNav.tsx:76`："接入新服务"按钮无 onClick。
- — `Composer.tsx:56-58`："安全策略 ◌"按钮无 onClick。
- — `TopBar.tsx:12-14`：模型选择按钮无 onClick；模型名硬编码 `OperMind-Reasoner`，与
  `/models` 页真实生效 provider 脱节（真实生效值可从 `get_model_config_query` 取）。
- **修复思路**：TopBar 模型名改读真实生效配置（轻流程可做）；"接入新服务"与"安全策略"两个
  按钮当前无对应能力，应删除或加明确的"未开放"提示，不留静默死按钮。

### P1-2 server_tools 硬编码假数据，违反"诚实降级"原则
- **现象**：`backend/src/tools/server_tools.py:35-37、69-70、101-102、143-144、176-177`：psutil 缺失时返回硬编码假指标（"CPU 使用率: 65% (4 核)"等）。与 `host_metrics.py:3-5` 及全项目"不伪造数值"原则直接冲突。
- **修复思路**：改真实采集结果或如实返回"采集不可用"，禁止伪造数值。

### P1-3 回答无 Markdown 渲染 + 结构化面板空态文案
- **现象**：回答气泡纯文本（`WorkbenchPage.tsx:219`）；`diagnosis-result.test.tsx:75-90` 明确断言"不渲染 Markdown"——**测试锁死**而非疏漏；"服务未返回结构化…"4 连排空态是噪音。
- **修复思路**：引入轻量 Markdown 渲染（react-markdown 等，白名单 sanitize）；结构化字段为空时面板整体折叠或换诚实文案（"只读调查未产生结构化证据"），不逐字段渲染空态。
- 状态：—（`docs/backlog.md` B2/B3）

### P1-4 运行中无"正在思考"反馈，发送瞬间零反馈
- **现象**：发送后用户消息要等 SSE 回流才出现；发送按钮无 loading；queued/running 无"正在思考"文字（只有 TraceCard pulse 点）；创建会话仅 disable 无 spinner。
- **修复思路**：发送即乐观回显 + 进行中气泡（含阶段文字）；按钮 loading 态。
- 状态：—（`docs/backlog.md` B1）

### P1-5 对话框太短 ✅
- **现象**：`workbench.css:485-489` `rows={1}` + `min-height:62px` + `max-height:170px`，`padding-bottom:46px` 预留给工具条，可视输入区更小。截图 `img/对话框太短.png`。
- **修复思路**：提高默认高度（如 3-4 行）+ 自动增高。
- 状态：**✅ 已修复**（2026-08-09，工作区改动待提交）
  - `Composer.tsx` 加 `useLayoutEffect` + ref，按 `scrollHeight` 自动增高，上限 320px 后转内部滚动；
  - `rows={1}` → `rows={3}`；CSS `min-height` 62→104px、`max-height` 170→320px、`overflow-y:hidden`；
  - 验证：`typecheck` 通过、`test` 84 passed / 12 files、`build` 成功（2026-08-09 实测）。
  - 剩余观感确认需真实体验（截图对照 `img/对话框太短.png`）。

### P1-6 Composer 写死 "PostgreSQL / 只读" context-chips
- **现象**：`Composer.tsx` 两个 chip 写死，与当前调查目标服务无关（多服务会话也显示 PostgreSQL）——误导。
- **修复思路**：从当前会话绑定服务真实渲染，或移除。

### P1-7 模型设置页"半功能"：7 个开关只存 localStorage、可用模型写死
- **现象**：`ModelSettingsPage.tsx:61-69` "Agent 调用策略"7 个开关只存 localStorage（:110），文案却暗示对应后端 Coordinator 装配；"可用模型"网格写死常量（:42-49）标"云端示例"仍易误导；TopBar 模型名与真实生效 provider 脱节（见 P1-1）。
- **修复思路**：要么后端真的提供装配开关 API 并接线，要么删除页面改为展示真实生效配置（诊断/裁判模型）。需设计评审。

### P1-8 长期记忆在 v1 链路默认关闭 + 关键词检索占位
- **现象**：`backend/src/core/bootstrap.py:50-55` 产品路径 `enable_long_term_memory=False`（注释"避免多 Run 并发写同一 memory.json"）；`memory/long_term.py` 是 JSON 文件 + 关键词子串匹配，docstring 自述"生产环境会用向量数据库…面试时可以说"；标签抽取只认 3 个词（`core/agent.py:101-110`）。
- **修复思路**：面试考点级优化候选——并发写锁 + 默认开启 + 简单向量检索（sqlite-vec / numpy embedding）。走 Design → Review → 用户确认（WP2 候选 #1）。
- 状态：—（`docs/backlog.md` 相关 WP2 候选）

### P1-9 "最近同步 / 刚刚"硬编码、"0 个服务在线"误导
- **现象**：`ServiceCenterPage.tsx:121-124` "最近同步 / 刚刚"从未实际同步也显示；`WelcomePanel.tsx:34` 服务数默认值写死 3、"在线"实为"已注册"；加载中显示 0 且 `services_query` 无 isError 处理（接口失败静默吞错，`WorkbenchPage.tsx`）。
- **修复思路**：真实记录同步时间戳或移除；注册数=在线数文案修正；加载/错误态补齐。

### P1-10 知识链未接入 chain 顺序
- **现象**：`backend/src/core/graph.py:204-208` 链式顺序只跑 server→db→log，knowledge 仅 parallel 时参与——知识库在常规调查中不引用。
- **修复思路**：评估把 knowledge 加入 chain（或明确为 parallel-only 并如实标注）。设计评审后定。

### P1-11 提案审批 UI 体验：批准/执行无 loading、失败无重试、字段缺失整个面板消失
- **现象**：`ActionProposalPanel.tsx`：批准/执行按钮无 loading；失败后无任何重试入口（"该提案不能重试"）；`read_proposal` 全字段非空校验，任一缺失整卡消失无降级文案。
- **修复思路**：按钮 loading；失败态给"重新发起调查"链接；字段缺失时按可用字段降级渲染。

### P1-12 会话侧栏 "Ctrl K" 假提示、硬编码用户信息
- **现象**：`Sidebar.tsx:40` "Ctrl K"快捷键提示，全项目无键盘监听；"王志海/研发运维团队"硬编码（无登录体系）。
- **修复思路**：实现 Ctrl K 快速切换会话（小）或删除提示；硬编码信息标注"演示占位"或移除。
- **状态**：⏳（2026-08-12，P8 会话管理 #64）Ctrl K 已实现：侧栏搜索框 + document 级 `Ctrl+K` 聚焦 + Esc 清空，前端交互测试覆盖（`Sidebar.test.tsx`）；浏览器端到端复验待办，复验后标 ✅。

---

## P2 可缓（展示打磨）

### P2-1 Trace 事件类型被过滤
- `TraceCard.tsx` 只渲染 4 类事件，debate/reflection/report 事件不显示——编排质量组件有节点但用户看不到。

### P2-2 趋势图自绘 div 条形，无坐标轴/单位说明
- `ServiceDetailPage.tsx` 趋势图仅 title 悬浮，无刻度数值。

### P2-3 错误样式过重、toast 一闪而过
- 校验错误用红色 Alert（"调查问题不能为空"）；toast 1.8s 消失（ModelSettingsPage 等多处）。

### P2-4 归档会话提示"重新激活未实现"
- **状态：✅（2026-08-23，issue #96 / PR #97 CI 通过、待合并）**。
- 已补侧栏“最近会话 / 已归档”双视图、归档标题搜索/cursor 分页、同一 Session 幂等恢复、失败事实回读与缓存收敛；归档详情保留历史、Run、提案和导出，仅限制会话录入。
- 本地 headless Chrome 端到端复验通过：归档列表进入详情 → 确认恢复 → 同一 session id 返回 active → Composer 恢复；无页面或 API 错误。

### P2-5 "刷新状态/重新检查"无真实结果反馈
- `ServiceCenterPage` / `ServiceDetailPage` 刷新按钮 refetch 后无成功/失败展示。

### P2-6 "服务在线"绿点常亮
- `TopBar.tsx` 绿点永远亮，无真实健康检查联动。

### P2-7 模型模式跳转靠 scrollIntoView 锚点
- `ServiceContextNav.tsx` 4 个跳转按钮无路由变化，刷新即失效。

### P2-8 主键 ID/时间戳等英文直出
- `DiagnosisResultPanel.tsx` 严重度徽章等标签英文直出，缺中文映射。

### P2-9 服务中心入口无状态徽标
- **现象**：左侧图标轨 `GlobalNav.tsx` 的"服务中心"入口只有 database 图标，无任何服务健康/数量角标（P0-5 遗留记入）。
- **修复思路**：需 Design——徽标承载什么信息（在线服务数？异常数？）、展示形态（角标/红点）、轮询来源（服务概览接口）。
- 状态：—（2026-08-10 用户决策记录，后续做）

---

## P7 功能体验验证（已在 main，需真实体验确认）

> 2026-08-09：#46–#51 全部已合入 main 且工作区已是最新，不再是"待 merge"。
> 这三项不是缺陷，是**待用真实体验确认没问题**的新功能。

- **P7-A 文档知识库页面**（#48/#49）：`frontend/src/features/knowledge/KnowledgePage.tsx` + `backend/src/knowledge/reader.py`。验证点：目录读取、关键词检索、`OPERMIND_KNOWLEDGE_DIR` 未配时的诚实空态。
- **P7-B 服务监控概览页**（#50/#51）：`frontend/src/features/monitor/MonitoringOverviewPage.tsx` + `backend/src/application/monitoring.py`。验证点：多实例聚合卡片、趋势摘要、DSN 未配时的诚实降级。
- **P7-C 锁与连接池只读诊断**（#46/#47）：`backend/src/tools/db_tools.py` 新增只读工具。验证点：会话中提锁等待/连接池问题能触发对应工具并产出证据。

---

## 验证基线（每天起服务前快速检查）

当前实测基线（2026-08-09，含工作区未提交改动）：

| 检查项 | 命令 | 当前基线 |
|---|---|---|
| 后端全量 | `..\.venv\Scripts\python.exe -m pytest tests -q`（在 `backend/`） | **338 passed, 2 skipped** |
| 前端类型 | `npm run typecheck` | 通过 |
| 前端测试 | `npm run test` | **84 passed / 12 files** |
| 前端构建 | `npm run build` | 成功 |

数字下降就是回归，别放过。此外每天起服务时确认：

- 后端启动无异常；`GET /health` 返回的 mode 与预期一致（mock / 真实）
- mock 模式：发 S1 慢查询问题 → Trace 完整 → 结论落地
- 真实模式：隧道通 → 服务 healthy → 调查 → 结构化字段有值（P0-1 ③ / P0-2 的验收点，**尚未通过**）
- 提交前 `git diff --check` 干净，只暂存本次改动文件

---

## 已修复记录

| 日期 | 条目 | 改动 | 验证方式 | 提交状态 |
|---|---|---|---|---|
| 2026-08-09 | P0-1 ①② | 两处 `to_regclass` 改 `is None` 判断 + 测试 mock 校正 + 4 个收集器用例 | `pytest tests/test_p5_controlled_action.py -q` → 11 passed | 未提交 |
| 2026-08-09 | P0-2 部分 | 收集器产出 `risks` 确定性范围说明，assembler 透传 + 2 个用例 | 同上 | 未提交 |
| 2026-08-09 | P1-5 | Composer 自动增高（3 行起、上限 320px）+ CSS 同步 | 前端三件套全绿（见下方基线） | 未提交 |
| 2026-08-09 | 清单外 | 删除侧栏"运维中心"入口卡（`Sidebar.tsx` + `app-shell.css` 59 行样式），运维中心统一走最左全局导航 | 前端三件套全绿 | 未提交 |
| 2026-08-09 | 文档 | 补 `开发规范.md` §7 完善流程；重写 `docs/README.md` 三层索引；`架构与开发路径.md` 按 P0–P7 实际状态更新 | 交叉引用自检 | 未提交 |

> 注：上表"未提交"指改动在工作区尚未 commit。按 `开发规范.md`，提交前需运行相关测试、
> `git diff --check`，且只暂存本次改动文件。
