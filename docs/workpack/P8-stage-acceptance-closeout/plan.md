# Issue #105 完善阶段验收与仓库收口 · 实施计划

> 流程：`docs/开发规范.md` §7.1 轻流程；不新增 API、迁移、Connector 或动作能力。
> 状态：实施、体验复验与最终全量验证完成，等待交付授权。

## 边界

- 复验 P7 文档知识库、监控概览、锁等待与连接池诊断三项既有能力。
- 以隔离 SQLite、mock LLM 和显式清空服务 DSN 的本地环境验证 UI 与主链，不把 mock 描述成真实外部执行。
- 真实受控动作只复用 issue #100 / #101 的已批准、已验证证据；当前目标已处于固定索引存在的安全后置状态，不为重跑删除索引。
- 修复验收中发现的既有会话路由缺口，并增加后端回归测试。
- 更新完善清单、跑通验证、路线图与工作包索引；只清理已合并且补丁等价的旧分支、干净 worktree 和重复活跃记录。
- 保留主工作区与 P6 worktree 的用户未提交改动。

## 切片

- [x] S1：P7 三项功能浏览器复验及 mock 主链复验。
- [x] S2：修复锁等待/连接池正式会话路由和 mock 工具选择，增加正负向测试。
- [x] S3：复核真实受控动作证据与当前只读后置状态，不触发破坏性回滚。
- [x] S4：归档 #100/#101/#102 工作包，清理已合并分支/worktree 残留。
- [x] S5：后端全量、ruff、mypy，前端 typecheck/test/build，`git diff --check`。
- [ ] S6：自审通过后等待提交、push、PR 与合并授权。

## 验收条件

- P7 三项页面/交互均可达，未配置状态诚实，Trace 不泄露原始工具输出或隐藏推理。
- “检查锁等待”“检查数据库锁”“检查连接池”进入调查路径；mock 下分别调用对应只读工具。
- mock 多角色主链完成，真实受控动作闭环证据可追溯，当前复核不产生目标写入。
- 文档与仓库状态一致；最终检查全部通过。
