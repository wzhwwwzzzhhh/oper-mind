# ===== 核心运行时依赖（2026-07-18 已验证环境） =====
# 在 Python 3.11.9 虚拟环境中安装，并于 2026-07-18 执行 pytest tests -q：34 passed。
openai==2.46.0
fastapi==0.139.2
uvicorn==0.51.0
pydantic==2.13.4
langgraph==1.2.9
langchain-openai==1.3.5
langchain-core==1.4.9
typing-extensions==4.16.0

# ===== 配置与本机诊断 =====
PyYAML==6.0.3       # src/config.py 读取 YAML 配置
psutil==7.2.2       # Server Agent 采集 CPU、内存、磁盘、进程和网络指标；缺失时回退 mock

# ===== 实验评测与统计（M3） =====
numpy==2.4.6        # src/eval/stats.py 数值计算
scipy==1.17.1       # src/eval/stats.py 的 t 分布、Wilcoxon 与 Friedman 检验

# ===== 开发、测试与文档脚本 =====
pytest==9.1.1       # tests/ 测试套件
fpdf==1.7.2         # scripts/ 生成方案 PDF

# ===== 应用持久化与迁移（P1.1d） =====
SQLAlchemy==2.0.51  # 同步 ORM；P2 应用元数据持久化基础
alembic==1.18.5     # 显式 schema migration；服务启动不自动迁移
psycopg[binary]==3.3.4  # PostgreSQL 应用数据库驱动；本地测试仍使用 SQLite

# ===== 真实诊断数据源（后续接入 MySQL 时启用） =====
# pymysql>=1.1.0
redis==8.1.0  # Redis 只读服务 Connector（P6）
