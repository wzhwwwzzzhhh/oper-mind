# ===== Core runtime dependencies (verified 2026-07-18) =====
# Installed in a Python 3.11.9 venv; `pytest tests -q` -> 34 passed on 2026-07-18.
# NOTE: keep this file ASCII-only so pip works under Windows GBK locale.
openai==2.46.0
fastapi==0.139.2
uvicorn==0.51.0
pydantic==2.13.4
langgraph==1.2.9
langchain-openai==1.3.5
langchain-core==1.4.9
typing-extensions==4.16.0

# ===== Config & local diagnostics =====
PyYAML==6.0.3       # src/config.py reads YAML config
psutil==7.2.2       # Server Agent collects CPU/memory/disk/process/network metrics; falls back to mock when missing

# ===== Experiment eval & stats (M3) =====
numpy==2.4.6        # src/eval/stats.py numeric computation
scipy==1.17.1       # src/eval/stats.py t-distribution / Wilcoxon / Friedman tests

# ===== Dev, test & doc scripts =====
pytest==9.1.1       # tests/ suite
fpdf==1.7.2         # scripts/ generate proposal PDFs
mypy==2.3.0         # static type check (per-layer config in pyproject.toml)
ruff==0.16.2        # linter/formatter (rules in pyproject.toml)

# ===== App persistence & migrations (P1.1d) =====
SQLAlchemy==2.0.51  # sync ORM; base for P2 app metadata persistence
alembic==1.18.5     # explicit schema migration; app does NOT auto-migrate on startup
psycopg[binary]==3.3.4  # PostgreSQL app DB driver; local tests still use SQLite
cryptography==44.0.0  # P6 model Provider API Key AES-256-GCM encryption
httpx==0.28.1       # P6 Provider connection verify HTTP client (also used by FastAPI TestClient)

# ===== Real diagnosis data sources (enable when MySQL connects later) =====
# pymysql>=1.1.0
redis==8.1.0  # Redis read-only service Connector (P6)
